import type { EventInstance } from "./types";

/**
 * Resolve a run's seeded event schedule. Task 3 fills in the act slot scripts,
 * per-family draws, and valley spacing; the seam returns an empty schedule so the
 * engine's tick pipeline and act machine run with no events in play.
 */
export function buildSchedule(_seed: number): EventInstance[] {
  return [];
}
